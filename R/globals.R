globalVariables(unique(c(
  # get_general_evaluation:
  "ESTUARY", "last_resultat", "last_year", "long_term_trend", "max_value", "PARAMETRE_LIBELLE", "short_last_trend", "status", "symbol", "trend", "y_pos",
  # get_general_levels:
  "ESTUARY", "median_ng_gww", "PARAMETRE_LIBELLE", "periode", "RESULTAT",
  # get_kruskal_periods_results:
  "ESTUARY", "median_1", "median_2", "PARAMETRE_LIBELLE", "periode", "RESULTAT",
  # get_last_trend_info:
  "ESTUARY", "last_trend", "PARAMETRE_LIBELLE", "res", "RESULTAT", "YEAR",
  # gg_map_data_contamination_rocchmv:
  "ESTUARY", "latitude", "LIEU_MNEMONIQUE", "longitude", "n", "YEAR",
  # gg_map_parametre_data_benthos:
  "ESTUARY", "label", "latitude", "longitude", "n", "YEAR"
)))
